<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rpm.ruchitesh.onlinetictactoe">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconnew"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconnew_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.rpm.ruchitesh.onlinetictactoe.MainActivity"></activity>
        <activity android:name="com.rpm.ruchitesh.onlinetictactoe.MainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>